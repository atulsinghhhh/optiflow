DATABASE_URL=postgres://postgres:postgres@localhost:5435/streamvault?sslmode=disable
JWT_SECRET=change-me-to-a-long-random-value
UPLOAD_SVC_PORT=8083
MINIO_ENDPOINT=localhost:9010
MINIO_ACCESS_KEY=streamvault
MINIO_SECRET_KEY=streamvault123
MINIO_USE_SSL=false
MINIO_BUCKET=streamvault
REDIS_ADDR=localhost:6381
FRONTEND_ORIGIN=http://localhost:3000
# Optional: local staging directory for in-progress tus (resumable) uploads.
# Defaults to $TMPDIR/streamvault-tus-uploads if unset. See plan.md's decision
# note on why this is local disk rather than direct-to-MinIO multipart.
TUS_DATA_DIR=
